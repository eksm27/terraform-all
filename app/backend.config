terraform {
  backend "s3" {
    bucket         = "state-v1"
    key            = "app/terraform.tfstate"
    region         = "ap-south-1"
    dynamodb_table = "terraform-app-locks"  # must exist
    encrypt        = true
  }
}